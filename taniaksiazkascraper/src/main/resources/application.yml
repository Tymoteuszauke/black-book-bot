server:
  port: 8104

endpoints:
  persistence-api: http://localhost:8101

const:
  pages-limit: 3

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %msg%n"
  file: taniaksiazkascraper.log

sring:
  output:
  ansi:
    enabled: DETECT